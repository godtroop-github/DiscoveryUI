import { Button, Result } from 'antd';
import React from 'react';
import { history } from 'umi';

const serviceFlowMonitor = () => (
  <div>serviceFlowMonitor</div>
);

export default serviceFlowMonitor;
